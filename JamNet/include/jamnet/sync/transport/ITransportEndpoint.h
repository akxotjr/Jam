#pragma once
#include "jamnet/core/net/Buffer.h"
#include "jamnet/runtime/world/WorldActionTypes.h"


namespace jam::net
{
	//class ITransportEndpoint
	//{
	//public:
	//	virtual ~ITransportEndpoint() = default;

	//	virtual void MulticastToAnotherWorld(const WorldKey& key, Packet packet) { (void)key; (void)packet; }
	//	virtual void Broadcast(Packet packet) { (void)packet; }
	//};
}
